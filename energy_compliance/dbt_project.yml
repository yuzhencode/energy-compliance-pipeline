name: 'energy_compliance'
version: '1.0.0'
config-version: 2

profile: 'energy_compliance'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  energy_compliance:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
